tunnel: atma-tunnel
credentials-file: /etc/cloudflared/cert.pem

ingress:
  # API Gateway - localhost:3000 -> api.futureguide.id
  - hostname: api.futureguide.id
    service: http://api-gateway:3000
    originRequest:
      httpHostHeader: api.futureguide.id
      
  # Documentation Service - localhost:8080 -> docs.futureguide.id  
  - hostname: docs.futureguide.id
    service: http://documentation-service:80
    originRequest:
      httpHostHeader: docs.futureguide.id
      
  # Catch-all rule (required)
  - service: http_status:404
